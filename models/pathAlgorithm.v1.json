{
  "title": "pathAlgorithm",
  "type": "object",
  "description": "Waypoint間のPath生成アルゴリズム",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "straight",
        "AStar",
        "navMesh",
        "graph"
      ]
    }
  },
  "required": [
    "type"
  ]
}